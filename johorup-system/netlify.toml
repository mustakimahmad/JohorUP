[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"

# Simple redirect for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
